# Vercel Configuration
# Using auto-detection for Create React App

# The project will be detected as a Create React App automatically
# API routes in /api folder will be detected as serverless functions
